import React from 'react'
import '../styles/App.css'

function AccountManager () {
    
    return(
        <div className='adminPanelContent'>
            <h2>Coming Soon!</h2>
            <p>
                This component will allow you to moderate user accounts. It will present
                some user account usage data, such as forum posts, deck posts and admin
                status. Admins can grant users admin credentials, mute, ban or block users
                for violating site policies. 
            </p>
        </div>       
    )
}

export default AccountManager